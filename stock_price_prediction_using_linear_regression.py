# -*- coding: utf-8 -*-
"""Stock Price Prediction using Linear Regression.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1cZ3wCgEzaMiHJN--VSfuxUK1CQUXn3qM
"""

!pip install Quandl

import pandas as pd
import numpy as np
import quandl
import math
from sklearn.linear_model import LinearRegression
from sklearn.preprocessing import scale
from sklearn.model_selection import train_test_split
from sklearn.metrics import mean_squared_error

df = quandl.get("EOD/AAPL", api_key='xeCRHnAqCK6_HqYGjbSr')

df['HL_PCT'] = (df['Adj_High']-df['Adj_Low'])/df['Adj_Close']*100.0

df['PCT_change'] = (df['Adj_Close']-df['Adj_Open'])/df['Adj_Open']*100.0

df = df.drop(['Open','High','Low','Close','Volume','Dividend','Split','Adj_Open','Adj_High','Adj_Low'],axis=1)

df['label'] = df['Adj_Close'].shift(-10)

df = df.dropna()
df.head()

clf = LinearRegression()
X = np.array(df.drop(['label'], 1))
y = np.array(df['label'])
X = scale(X)
X.shape

X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2)

clf.score(X_train,y_train)

y_pred = clf.predict(X_test)

MSE = mean_squared_error(y_test,y_pred)
MSE

